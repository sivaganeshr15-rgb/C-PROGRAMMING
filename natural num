#include <stdio.h>

int main() {
    int n;
    printf("Enter a positive integer (N): ");
    scanf("%d", &n);

    printf("Natural numbers up to %d:\n", n);
    for (int i = 1; i <= n; i++) {
        printf("%d ", i);
    }
    printf("\n");

    return 0;
}
